---
title: Plugins overview
content-type: lifecycle-map
assumes: []
canonical_for: [plugin]
---
# Plugins

This page is clean prose. Every pattern-shaped string below lives inside a fenced
code block (a documentation example) and must NOT be flagged. A reader isn't being
told any of it as fact; it's sample markdown.

```markdown
See [connectors](/docs/docs/connectors/overview), plus skills, hooks, and sub-agents.
Plugins are fully available in web Chat.
For more, see [the guide](https://code.claude.com/docs/en/plugins).
```

Outside the fence there is nothing to flag: this page owns the plugin concept and
links nothing problematic. It should verdict as Ship once the checker ignores
fenced code.
