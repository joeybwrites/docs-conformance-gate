---
title: Inline code is ignored
content-type: lifecycle-map
assumes: []
canonical_for: [plugin]
---
# Plugins

Set the `assumes` field. A path shown in code such as `/docs/docs/example` must not trip S6.

[Skills](/docs/skills/overview) and [connectors](/docs/connectors/overview) are linked on first mention.

<!-- No S6 (the doubled prefix is inside inline code) and no S4 (skill/connector
     are linked on first mention; the `code` spans carry no live concept). -> Ship. -->
