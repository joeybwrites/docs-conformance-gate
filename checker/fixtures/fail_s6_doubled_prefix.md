---
title: Plugins overview (S6 FAIL fixture — doubled link prefix)
content-type: lifecycle-map
assumes: []
canonical_for: [plugin]
---
# Plugins

See [connectors](/docs/docs/connectors/overview).

And [skills](/docs/docs/skills/overview).

<!-- Both links above carry the doubled prefix (they resolve via redirect
     but are non-canonical, duplicate URLs). S6 should fire on each such line.
     Note: the concepts are still "linked to their owner" substring-wise, so S4 stays clean. -->
